import { type Pan } from '../schema';

export async function getPans(): Promise<Pan[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all pans from the database.
    // Should return all pans with their availability status.
    return [];
}